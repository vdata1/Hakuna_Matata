/**
 * convert key path string to key array
 * @private
 * @param {String} path
 * @returns {Array}
 * @example
 * pathToKeys("selector.to[0][11].value"); // ["selector", "to", 0, 11, "value"]
 */
export declare const pathToKeys: (path: string) => (string | number)[];
/**
 *
 * @private
 * @param {*} obj
 * @param {String} key
 * @param {*} method
 * @example
 * addPrototype(Object, "$size", function() {return this;});
 */
export declare const addPrototype: (obj: any, key: string, method: (...args: any[]) => any) => void;
/**
 *
 * @private
 * @param {Array} arr
 * @param {String} [path]
 * @param {Function} fn
 * @returns {*}
 * @example
 * filter([1, 2, 3], undefined, (value) => value > 1);
 */
export declare const filter: <T>(arr: T[], path: string | undefined, fn: (value: T, index: number, array: T[]) => any) => T[];
/**
 *
 * @private
 * @param {*} value
 * @returns {*}
 * @example
 * deepClone([1, 2, 3]);
 */
export declare const deepClone: <T>(value: T) => T;
export declare type Operator = "<" | "<=" | "=" | "<>" | ">=" | ">";
export declare const enum OPERATOR {
    LT = "<",
    LTE = "<=",
    EQ = "=",
    NE = "<>",
    GTE = ">=",
    GT = ">"
}
export declare type PathsToStringPropsT<Value> = Value extends Record<string, unknown> ? {
    [Key in keyof Value]: [Key] | [Key, ...PathsToStringPropsT<Value[Key]>];
}[keyof Value] : [];
export declare type JoinT<Value extends string[]> = Value extends [] ? never : Value extends [infer F] ? F : Value extends [infer F, ...infer R] ? F extends string ? string extends F ? string : `${F}.${JoinT<Extract<R, string[]>>}` : never : string;
export declare type PathT<Value extends Record<string, unknown>> = JoinT<Extract<PathsToStringPropsT<Value>, string[]>>;
//# sourceMappingURL=utils.d.ts.map