declare global {
    namespace NodeJS {
        interface Global {
            isBrowser(): boolean;
        }
    }
    const isBrowser: () => boolean;
}
export {};
//# sourceMappingURL=shim.d.ts.map