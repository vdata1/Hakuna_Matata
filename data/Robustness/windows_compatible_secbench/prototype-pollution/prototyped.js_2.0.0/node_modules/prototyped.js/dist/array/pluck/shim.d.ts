import { PathT } from "../../utils";
declare global {
    interface Array<T> {
        pluck(path: T extends Record<string, unknown> ? PathT<T> : never): unknown[];
    }
}
//# sourceMappingURL=shim.d.ts.map