"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("../../utils");
exports.default = max;
function max(arr, path) {
    let reducer = (item) => item;
    if (path) {
        const keys = utils_1.pathToKeys(path);
        reducer = (item) => keys.reduce((prev, curr) => (prev && prev[curr]) || -Infinity, item);
    }
    return arr.reduce((prev, cur) => Math.max(prev, reducer(cur)), -Infinity);
}
//# sourceMappingURL=index.js.map