"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function distinctBy(array, fn) {
    const distinct = [];
    array.forEach((a) => {
        let had = false;
        for (let i = 0; i < distinct.length; i++) {
            if (!fn(a, distinct[i]))
                continue;
            had = true;
            break;
        }
        if (!had)
            distinct.push(a);
    });
    return distinct;
}
exports.default = distinctBy;
//# sourceMappingURL=index.js.map