import { PathT } from "../../utils";
declare global {
    interface Array<T> {
        implode(path: T extends Record<string, unknown> ? PathT<T> : string, separator?: string): string;
    }
}
//# sourceMappingURL=shim.d.ts.map