import { OrderT } from ".";
import { PathT } from "../../utils";
declare global {
    interface Array<T> {
        orderBy(order?: OrderT): T[];
        orderBy(field: T extends Record<string, unknown> ? PathT<T> : never, order?: OrderT): T[];
    }
}
//# sourceMappingURL=shim.d.ts.map