import { PathT } from "../../utils";
declare global {
    interface Array<T> {
        average(path?: T extends Record<string, unknown> ? PathT<T> : never): number;
        avg(path?: T extends Record<string, unknown> ? PathT<T> : never): number;
    }
}
//# sourceMappingURL=shim.d.ts.map