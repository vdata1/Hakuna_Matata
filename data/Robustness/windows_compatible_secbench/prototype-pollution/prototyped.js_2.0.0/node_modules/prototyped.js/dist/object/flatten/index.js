"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const forEach_1 = __importDefault(require("../forEach"));
const isPlainObject_1 = __importDefault(require("../isPlainObject"));
function flatten(obj) {
    const result = {};
    forEach_1.default(obj, (value, key) => {
        if (Array.isArray(value)) {
            const temp = {};
            value.forEach((subValue, index) => {
                temp[`[${index}]`] = subValue;
            });
            value = temp;
        }
        if (isPlainObject_1.default(value)) {
            const flat = flatten(value);
            forEach_1.default(flat, (subValue, subKey) => {
                const separator = /^\[/.test(subKey) ? "" : ".";
                result[`${key}${separator}${subKey}`] = subValue;
            });
            return;
        }
        result[key] = value;
    });
    return result;
}
exports.default = flatten;
//# sourceMappingURL=index.js.map