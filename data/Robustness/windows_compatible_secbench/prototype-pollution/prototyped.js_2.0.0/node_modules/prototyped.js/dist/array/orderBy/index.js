"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ORDER = void 0;
const utils_1 = require("../../utils");
const contains_1 = __importDefault(require("../contains"));
exports.default = orderBy;
function orderBy(arr, field, order = "asc" /* ASC */) {
    if (field && contains_1.default(["asc" /* ASC */, "desc" /* DESC */], field)) {
        order = field;
        field = undefined;
    }
    const iterator = order === "asc" /* ASC */
        ? (a, b) => (a > b ? 1 : a < b ? -1 : 0)
        : (a, b) => (a < b ? 1 : a > b ? -1 : 0);
    if (field) {
        const keys = utils_1.pathToKeys(field);
        const reducer = (item) => keys.reduce((prev, cur) => prev[cur], item);
        return arr.sort((a, b) => iterator(reducer(a), reducer(b)));
    }
    return arr.sort(iterator);
}
var ORDER;
(function (ORDER) {
    ORDER["ASC"] = "asc";
    ORDER["DESC"] = "desc";
})(ORDER = exports.ORDER || (exports.ORDER = {}));
//# sourceMappingURL=index.js.map